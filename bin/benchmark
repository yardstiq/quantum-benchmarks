#!/bin/bash
FILE_PATH="$( cd "$(dirname "$0")" >/dev/null 2>&1 ; pwd -P )"
ROOT_PATH=$(dirname "$FILE_PATH")

JULIA_PROJECT=$ROOT_PATH /usr/bin/env julia \
    --startup-file=no \
    -- $ROOT_PATH/bin/benchmark.jl $@
