#!/bin/sh
BASEDIR=$(dirname "$0")
JULIA_BIN="/usr/bin/env julia"
JULIA_PROJECT_DIR=$(dirname $BASEDIR)
JULIA_PROJECT=$JULIA_PROJECT_DIR $JULIA_BIN --color=yes -e "using Benchmark; Benchmark.main()" $1 $2
